"""Behavioral journal (Shadow Account) — parses a trade journal into a behavioral
profile + bias flags.

A subpackage of the **prototrader-finance** bundle: `tools.get_behavioral_tools()`
is composed by the bundle's top-level `register()`.
"""
