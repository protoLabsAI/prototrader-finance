"""Vectorized strategy backtesting — realistic costs, OOS split, buy-and-hold
benchmark, bootstrap Sharpe CI.

A subpackage of the **prototrader-finance** bundle: `tools.get_backtest_tools()`
is composed by the bundle's top-level `register()`, and `engine` is also driven by
the Quant Desk dashboard view (`../dashboard.py`).
"""
