"""Market-data tools (equities via yfinance, crypto via ccxt).

A subpackage of the **prototrader-finance** bundle — `tools.get_finance_tools()`
is composed by the bundle's top-level `register()` (see `../__init__.py`). Tools
degrade to a clear "install requirements-finance.txt" error when deps are absent.
"""
