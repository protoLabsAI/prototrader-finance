"""Factor evaluation — a tractable Alpha Zoo: price/volume factors IC-scored over
a universe.

A subpackage of the **prototrader-finance** bundle: `tools.get_factor_tools()` is
composed by the bundle's top-level `register()`.
"""
